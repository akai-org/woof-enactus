{
  "name": "woof-enactus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:dev:db": "docker compose -f compose.dev.yml up -d",
    "stop:dev:db": "docker compose -f compose.dev.yml down",
    "start:dev:server": "npm -w server run start:dev",
    "start:dev:client": "npm -w client run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^3.5.1"
  },
  "dependencies": {
    "@hookform/resolvers": "^5.0.1"
  }
}
